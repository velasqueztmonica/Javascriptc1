//Convert Minutes into Seconds
// Write a function that takes an integer minutes and converts it to seconds.

function minutestsec(minutes) {
    return minutes * 60
}
console.log(minutestsec(10))
// ES6 Function format

mtoseconds2 = (minutes2) => {
    return minutes2 * 60;
}

console.log(mtoseconds2(5))