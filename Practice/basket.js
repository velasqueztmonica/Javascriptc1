// Basketball Points
// You are counting points for a basketball game, given the amount of 2-pointers scored and 3-pointers scored, 
//find the final points for the team and return that value.

function basketpoints(num1, num2) {
    return (num1 * 2) + (num2 *3)
}

console.log(basketpoints(4, 5));

const basketp = (num3, num4) => {
    return (num3 * 2) + (num4 *3)
}

console.log(basketp(4,5))